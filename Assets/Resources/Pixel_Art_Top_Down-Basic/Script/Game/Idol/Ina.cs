using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ina : Prototype
{
    // Start is called before the first frame update
    void Start()
    {
        X = 7;
        Y = 3;
        HP = 1900;
        ATK = 470;
        dis = 2;
        dis_type = 1;
        throwable = 0;
        move = 1;
        move_type = 1;
        fly = 1;
        belong = 1;
        moving_rate = 0.01f;
    }

    // Update is called once per frame

}
